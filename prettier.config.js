module.exports = {
  printWidth: 80, // Specifies the line length that Prettier will wrap.
  tabWidth: 2, // Number of spaces per indentation level.
  useTabs: false, // Indent lines with tabs instead of spaces.
  semi: true, // Print semicolons at the ends of statements.
  // singleQuote: false,         // Use single quotes instead of double quotes.
  quoteProps: "as-needed", // Change when properties in objects are quoted.
  jsxSingleQuote: false, // Use single quotes instead of double quotes in JSX.
  trailingComma: "es5", // Print trailing commas wherever possible (valid in ES5).
  bracketSpacing: true, // Print spaces between brackets in object literals.
  bracketSameLine: true,
  // jsxBracketSameLine: false,  // Put `>` of a multi-line JSX element at the end of the last line.
  arrowParens: "always", // Include parentheses around a sole arrow function parameter.
  endOfLine: "auto", // Specify the line ending to be used (lf, crlf, cr, auto).
};

